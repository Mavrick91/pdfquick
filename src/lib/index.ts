/**
 * Barrel export for shared library utilities.
 * ---------------------------------------------------------------
 * Enables cleaner import paths, e.g.:
 *   import { formatFileSize } from "@/lib";
 *
 * Add additional re-exports here as new helpers are introduced.
 */

export * from "./pdf-utils";
