/**
 * @deprecated Import { formatFileSize } from "@/lib/pdf-utils" instead.
 *
 * This module now simply re-exports the shared helper to avoid breaking
 * existing import paths while we migrate code.
 */
export { formatFileSize } from "./pdf-utils";
